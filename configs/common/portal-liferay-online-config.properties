# Intentionally empty: shadows the DXP 2026 base image's /home/liferay/portal-liferay-online-config.properties.
# Why: the baked-in file sets json.servlet.hosts.allowed=N/A which blocks JSONWS from localhost.
